# 测试配置文件
server:
  port: demo
spring:
  # 数据库配置
  # todo 需替换配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: demo
    username: demo
    password: demo
  # Redis 配置
  # todo 需替换配置
  redis:
    database: 1
    host: demo
    port: demo
    timeout: 5000
    password: demo
